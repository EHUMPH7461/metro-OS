import { X } from 'lucide-react';
import type { InventoryInput, InventoryStatus } from '../../../shared/inventory';
import { calculateItemFinancials } from '../../domain/inventory';

const money=(v:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(v);
const fields:Array<{key:keyof InventoryInput;label:string;type?:string}>=[{key:'title',label:'Title'},{key:'brand',label:'Brand'},{key:'category',label:'Category'},{key:'gender',label:'Gender'},{key:'size',label:'Size'},{key:'color',label:'Color'},{key:'condition',label:'Condition'},{key:'quantity',label:'Quantity',type:'number'},{key:'purchasePrice',label:'Purchase price',type:'number'},{key:'listPrice',label:'List price',type:'number'},{key:'shippingCost',label:'Shipping cost',type:'number'},{key:'ebayFees',label:'eBay fees',type:'number'},{key:'bin',label:'Bin'},{key:'rack',label:'Rack'},{key:'shelf',label:'Shelf'},{key:'drawer',label:'Drawer'},{key:'supplier',label:'Supplier'},{key:'purchaseDate',label:'Purchase date',type:'date'},{key:'listingDate',label:'Listing date',type:'date'},{key:'soldDate',label:'Sold date',type:'date'},{key:'ebayItemId',label:'eBay item ID'}];
export function InventoryModal({form,onChange,onClose,onSave,saving}:{form:InventoryInput;onChange:(v:InventoryInput)=>void;onClose:()=>void;onSave:()=>void;saving:boolean}) { const finances=calculateItemFinancials(form); return <div className="modal-backdrop"><div className="modal"><div className="modal-title"><div><h2>Add inventory item</h2><p>SKU is generated automatically if left blank.</p></div><button className="icon" onClick={onClose} aria-label="Close"><X/></button></div><div className="form-grid"><label>SKU<input value={form.sku} onChange={e=>onChange({...form,sku:e.target.value})} placeholder={`MRR-${new Date().getFullYear()}-000001 (auto)`}/></label>{fields.map(({key,label,type})=><label key={key}>{label}<input type={type??'text'} step={type==='number'?'0.01':undefined} value={String(form[key])} onChange={e=>onChange({...form,[key]:type==='number'?Number(e.target.value):e.target.value})}/></label>)}<label>Status<select value={form.status} onChange={e=>onChange({...form,status:e.target.value as InventoryStatus})}><option>Draft</option><option>Active</option><option>Sold</option><option>Archived</option></select></label><label className="wide">Notes<textarea value={form.notes} onChange={e=>onChange({...form,notes:e.target.value})}/></label></div><div className="profit-preview"><div><span>Gross Profit</span><strong>{money(finances.grossProfit)}</strong></div><div><span>Net Profit</span><strong>{money(finances.netProfit)}</strong></div><div><span>ROI</span><strong>{finances.roi.toFixed(1)}%</strong></div></div><div className="modal-actions"><button onClick={onClose}>Cancel</button><button className="primary" disabled={saving} onClick={onSave}>{saving?'Saving…':'Save Item'}</button></div></div></div>; }
