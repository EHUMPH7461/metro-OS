import{describe,expect,it}from'vitest';import{validateAnalyticsExport}from'./analytics-export.js';describe('analytics export validation',()=>{it('creates a dated safe filename',()=>expect(validateAnalyticsExport('category','a,b\r\n').fileName).toMatch(/^metro-analytics-category-\d{4}-\d{2}-\d{2}\.csv$/));it('rejects arbitrary export kinds',()=>expect(()=>validateAnalyticsExport('../x','a')).toThrow('Unsupported'));it('rejects empty exports',()=>expect(()=>validateAnalyticsExport('brand','')).toThrow('invalid'))});
