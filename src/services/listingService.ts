import type{ListingInput,ListingQueueItem}from'../../shared/listings';import{unwrap}from'./inventoryService';export const listingService={async queue():Promise<ListingQueueItem[]>{if(!window.metro)return[];return unwrap(await window.metro.listings.queue())},async save(inventoryId:number,input:ListingInput){if(!window.metro)throw new Error('Listing storage is unavailable.');return unwrap(await window.metro.listings.save(inventoryId,input))}};
